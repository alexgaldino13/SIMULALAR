# Tarefa 3: Configuração de Build e Metadados (Lola Lançamento)

**Objetivo:** Configurar o projeto Expo para builds de produção.

**Instruções:**
1. Revise o `mobile/app.json` e garanta que o `android.package` e `ios.bundleIdentifier` estão corretos.
2. Configure o `mobile/eas.json` com um perfil de `production`.
3. Garanta que a `versionCode` está preparada para o primeiro upload.

**Saída esperada:** Arquivos de configuração atualizados.
