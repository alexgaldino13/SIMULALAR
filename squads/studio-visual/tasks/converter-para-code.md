# Task: Converter para Código React Native
Objetivo: Transformar a especificação visual em um arquivo funcional `.tsx`.

## Instruções
1. Leia o arquivo `output/design-blueprint.md`.
2. Crie uma nova tela em `mobile/src/screens/`.
3. Use o `SimulationContext` se a tela precisar de dados financeiros.
4. Garanta que o layout seja responsivo e use `SafeAreaView`.

## Output esperado
O arquivo `.tsx` criado e um relatório de integração em `output/code-report.md`.
